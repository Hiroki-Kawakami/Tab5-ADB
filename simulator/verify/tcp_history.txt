# HomeScreen Wireless card: recent ADB-over-TCP targets list.
# Seed the targets first by writing tab5adb/tcp_history into the gitignored
# nvs_data.json (newline-joined host:port, hex-encoded incl. the trailing NUL,
# most-recent-first) — the sim NVS stores values as hex. With history present the
# "Recent" list renders; tapping a row fills the address field with that target.
wait 600
settle
capture simulator/verify/out/tcp_history.jpg
# Tap a recent row (third entry, 10.0.0.7:5555) — fills the address field with it.
tap 200 886
settle
capture simulator/verify/out/tcp_history_selected.jpg
quit
