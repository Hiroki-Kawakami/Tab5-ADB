# Measure mirror FPS: connect -> open Mirroring -> stream for ~25s so the
# per-second fps log on stdout reaches steady state. (Temp script for the
# uncapped-fps measurement.)
settle
tap 360 420
wait 9000
settle
# Open the Mirroring tool (top-right tool button).
tap 204 400          # tap the preview image -> Mirroring screen
# Let the agent launch (~8s) then stream long enough for a clean steady read.
wait 30000
capture simulator/verify/out/mirror_fps.jpg
quit
