# End-to-end mirror: connect -> open Mirroring -> launch agent -> show the stream.
settle
tap 360 640
wait 6000
settle
# Device screen is up; open the Mirroring tool (top-right tool button).
tap 552 162
# Launching the agent (pkill + push jar + app_process + link + MIRROR_START) and
# the first frames take several seconds; capture progressively.
wait 8000
capture simulator/verify/out/mirror_8s.jpg
wait 6000
capture simulator/verify/out/mirror_14s.jpg
wait 4000
capture simulator/verify/out/mirror_18s.jpg
quit
