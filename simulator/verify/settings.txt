# Settings screen: Home -> Settings nav card -> Display + Audio blocks.
# Exercises the brightness slider (incl. 0/100% knob clipping), the color-mode
# toggle (restart modal), and the audio block: master volume slider, Speaker
# Output (Off|Auto) and Equalizer (Off|On) toggles, and the Sound Playback
# dropdown. The script never confirms Restart — that would exit(0) the simulator.
settle
capture simulator/verify/out/settings_home.jpg
tap 589 1170
settle
capture simulator/verify/out/settings_open.jpg

# --- Brightness slider (Display) ---
tap 200 418
settle
capture simulator/verify/out/settings_brightness.jpg
# Extremes: grab the knob and drag past each end (the slider clamps to 1%/100%)
# so the knob must not clip. Waits let the drag samples register.
down 200 418
wait 60
move 40 418
wait 60
up
settle
capture simulator/verify/out/settings_min.jpg
down 200 418
wait 60
move 700 418
wait 60
up
settle
capture simulator/verify/out/settings_max.jpg

# --- Audio block ---
# Lower the master volume via a drag.
down 457 737
wait 60
move 200 737
wait 60
up
settle
# Speaker Output -> Off (left pill), Equalizer -> Off (left pill).
tap 536 818
settle
tap 553 905
settle
capture simulator/verify/out/settings_audio.jpg
# Sound Playback dropdown -> open, pick "Android Device".
tap 534 989
settle
tap 495 1084
settle
capture simulator/verify/out/settings_playback.jpg

# --- Color mode toggle (16-bit; default is 24-bit) -> restart-required modal ---
tap 511 502
settle
capture simulator/verify/out/settings_color.jpg
quit
