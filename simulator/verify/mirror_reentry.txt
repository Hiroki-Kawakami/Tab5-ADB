# Re-entry: open Mirroring (lazy agent launch) -> Back -> open Mirroring again.
# The 2nd open must REUSE the live agent (no pkill/push/launch) — AgentClient keeps
# the link connected across the screen's onExit (stop_mirror only).
settle
tap 360 420
wait 9000
settle
# Device screen up: open Mirroring (1st time -> full bring-up).
tap 204 400          # tap the preview image -> Mirroring screen
wait 10000
capture simulator/verify/out/reentry_1.jpg
# End (mirroring終了 = top icon of the bottom-left control strip) -> pops the
# screen: stop_mirror, keep the agent connected.
tap 40 634
wait 3000
settle
# Open Mirroring again (2nd time -> should resume instantly, no re-launch).
tap 204 400          # tap the preview image -> Mirroring screen
wait 6000
capture simulator/verify/out/reentry_2.jpg
quit
