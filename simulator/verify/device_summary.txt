# ADBDeviceScreen summary header: Connect -> device screen; the chained-exec
# summary (device name / model • Android • storage / battery + Wi-Fi (+ cell)
# icons) lands within a round trip of onAppear. Needs a real phone on the PC.
settle
tap 360 640
wait 9000
settle
wait 2500
capture simulator/verify/out/device_summary.jpg
quit
